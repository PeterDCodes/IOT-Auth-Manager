A basic device registration service that is intended for use in mobile devices that are not tied to a specific user or account.

Will be built out for primary use in Android applications that require authenticated API calls
